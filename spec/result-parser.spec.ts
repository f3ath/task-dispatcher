import { OutputParser } from "../src/output-parser";
import { RunResult } from "../src/run-result";

// class ModuleResultParser implements OutputParser{
//   parse(runtime: number, code: number, stdout: string, stderr: string): RunResult {
//     return {
//       runtime: runtime,
//       exitCode: code,
//       passCount: 0,
//       failCount: 0,
//       errors: []
//     };
//   }
// }

describe('OutputParser', () => {
  // const parser = new ModuleResultParser();
  it('parses all positive', () => {

  })

});